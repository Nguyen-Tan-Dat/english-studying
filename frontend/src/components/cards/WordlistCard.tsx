import{Link}from'react-router-dom';import type{TopicTree}from'../../api/types';import{Badge}from'../common/Badge';export function WordlistCard({tree}:{tree:TopicTree}){const state=tree.revision.state;return <Link className="tree-card" to={`/trees/${tree.id}`}><div className="tree-card-top"><span className="tree-icon">🌳</span><Badge tone={state==='PUBLISHED'?'success':state==='DRAFT'?'warning':'neutral'}>{state}</Badge></div><h3>{tree.display_name}</h3><p>{tree.description||'Chưa có mô tả'}</p><div className="tree-meta"><span>{tree.node_count??0} chủ đề</span><span>{tree.vocabulary_count??0} từ</span></div></Link>}
