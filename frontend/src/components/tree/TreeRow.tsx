import type{TopicNode}from'../../api/types';import{Badge}from'../common/Badge';export function TreeRow({node,selected,onSelect,onExpand,expanded,level=1}:{node:TopicNode;selected:boolean;onSelect:()=>void;onExpand:()=>void;expanded:boolean;level?:number}){return <div className={`tree-row ${selected?'selected':''}`} role="treeitem" aria-level={level} aria-selected={selected} aria-expanded={node.node_type==='GROUP'?expanded:undefined} style={{paddingLeft:`${12+(level-1)*22}px`}}><button className="tree-expand" onClick={onExpand} disabled={!node.has_children}>{node.has_children?(expanded?'▾':'▸'):'·'}</button><button className="tree-main" onClick={onSelect}><span className={`node-symbol ${node.node_type.toLowerCase()}`}>{node.node_type==='GROUP'?'▰':'Aa'}</span><span><strong>{node.display_name}</strong><small>{node.path_text}</small></span></button><Badge tone={node.node_type==='GROUP'?'warning':'info'}>{node.node_type}</Badge><span className="tree-count">{node.vocabulary_count} từ</span></div>}
