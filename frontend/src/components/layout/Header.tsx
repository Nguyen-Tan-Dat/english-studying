import{useAuth}from'../../hooks/useAuth';export function Header(){const{user,logout}=useAuth();return <header className="app-header"><div><strong>Xin chào, {user?.display_name}</strong><span>Hãy hoàn thành mục tiêu hôm nay.</span></div><div className="header-actions"><button className="icon-button" aria-label="Thông báo">🔔</button><span className="avatar">{user?.display_name?.slice(0,1).toUpperCase()}</span><button className="logout" onClick={()=>logout()}>Đăng xuất</button></div></header>}
