import{Navigate,Outlet}from'react-router-dom';import{useAuth}from'../hooks/useAuth';import{Spinner}from'./common/Spinner';export function ProtectedRoute(){const{user,loading}=useAuth();if(loading)return <div className="center-screen"><Spinner/></div>;return user?<Outlet/>:<Navigate to="/login" replace/>}export function AdminRoute(){const{user}=useAuth();return user?.roles.includes('ADMIN')?<Outlet/>:<Navigate to="/dashboard" replace/>}
