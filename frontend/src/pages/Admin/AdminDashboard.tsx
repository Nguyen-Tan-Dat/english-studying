import{useQuery}from'@tanstack/react-query';import{adminApi}from'../../api/endpoints/admin.api';import{Spinner}from'../../components/common/Spinner';export default function AdminDashboard(){const dashboard=useQuery({queryKey:['admin','dashboard'],queryFn:adminApi.dashboard});const health=useQuery({queryKey:['admin','health'],queryFn:adminApi.health});const audit=useQuery({queryKey:['admin','audit'],queryFn:adminApi.audit});if(dashboard.isLoading)return <Spinner/>;return <div className="admin-page"><div className="page-title"><div><span className="eyebrow admin">ADMIN CONSOLE</span><h1>Vận hành nội dung</h1><p>Theo dõi publish, chất lượng dữ liệu, import và audit.</p></div></div><section className="admin-stats"><article><small>Cây bản nháp</small><strong>{dashboard.data?.draft_trees??0}</strong></article><article><small>Đã xuất bản</small><strong>{dashboard.data?.published_trees??0}</strong></article><article><small>Vấn đề nội dung</small><strong>{dashboard.data?.content_issues??0}</strong></article><article><small>Import đang chờ</small><strong>{dashboard.data?.pending_imports??0}</strong></article></section><div className="admin-grid"><section className="panel"><h2>Content Health</h2><dl className="health-list">{Object.entries(health.data??{}).map(([key,value])=><div key={key}><dt>{key.replaceAll('_',' ')}</dt><dd>{String(value)}</dd></div>)}</dl></section><section className="panel"><h2>Audit gần đây</h2><div className="audit-list">{audit.data?.items?.slice(0,8).map((item:any)=><article key={item.id}><span className="audit-dot"/><div><strong>{item.action}</strong><p>{item.resource_type} · {new Date(item.created_at).toLocaleString('vi-VN')}</p></div></article>)}</div></section></div></div>}
