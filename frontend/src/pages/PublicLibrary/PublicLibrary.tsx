import{useState}from'react';import{useMutation,useQuery}from'@tanstack/react-query';import{libraryApi}from'../../api/endpoints/public-library.api';import{Input}from'../../components/common/Input';import{Button}from'../../components/common/Button';import{Badge}from'../../components/common/Badge';import{problemMessage}from'../../api/client';export default function PublicLibrary(){const[q,setQ]=useState('');const[msg,setMsg]=useState('');const list=useQuery({queryKey:['library',q],queryFn:()=>libraryApi.list({q,page_size:50})});const clone=useMutation({mutationFn:(id:string)=>libraryApi.clone(id),onSuccess:(op)=>setMsg(`Đã clone thành công. Operation: ${op.id}`),onError:e=>setMsg(problemMessage(e))});return <div><div className="page-title"><div><h1>Thư viện công khai</h1><p>Khám phá và clone các cây hoặc nhánh do cộng đồng xuất bản.</p></div></div><div className="toolbar"><Input placeholder="Tìm nội dung công khai…" value={q} onChange={e=>setQ(e.target.value)}/></div>{msg&&<div className="notice success">✓ {msg}</div>}<div className="library-grid">{list.data?.items.map(item=><article className="library-card" key={item.id}><div className="library-cover"><span>🌍</span><Badge tone="success">{item.scope}</Badge></div><h3>{item.display_name}</h3><p>{item.description}</p><div className="tree-meta"><span>{item.node_count} Topic</span><span>{item.vocabulary_count} từ</span><span>{item.clone_count} clone</span></div><Button onClick={()=>clone.mutate(item.id)} disabled={clone.isPending}>Clone vào kho của tôi</Button></article>)}</div></div>}
