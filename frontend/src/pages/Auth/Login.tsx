import{useState,type FormEvent}from'react';import{Navigate,useLocation,useNavigate}from'react-router-dom';import{useAuth}from'../../hooks/useAuth';import{Input}from'../../components/common/Input';import{Button}from'../../components/common/Button';import{problemMessage}from'../../api/client';
export default function Login(){const{user,login}=useAuth();const nav=useNavigate();const location=useLocation();const[email,setEmail]=useState('learner@lexigo.local');const[password,setPassword]=useState('Demo123!');const[error,setError]=useState('');const[busy,setBusy]=useState(false);if(user)return <Navigate to="/dashboard" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password);nav((location.state as any)?.from??'/dashboard',{replace:true})}catch(err){setError(problemMessage(err))}finally{setBusy(false)}}return <div className="auth-page"><section className="auth-brand"><div className="brand"><span className="brand-mark">L</span><span>LexiGo</span></div><div><span className="eyebrow">Học thông minh hơn</span><h1>Biến từ vựng thành kỹ năng sử dụng thật.</h1><p>Cây chủ đề đa tầng, ôn tập theo tiến độ và bảy chế độ luyện tập trong một trải nghiệm rõ ràng.</p></div><div className="auth-visual"><span>🍎</span><strong>apple</strong><small>/ˈæpəl/ · quả táo</small></div></section><section className="auth-form"><form onSubmit={submit}><h2>Chào mừng trở lại</h2><p>Đăng nhập để tiếp tục hành trình học.</p>{error&&<div className="notice error">⚠ {error}</div>}<Input label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><Input label="Mật khẩu" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><Button disabled={busy} type="submit">{busy?'Đang đăng nhập…':'Đăng nhập'}</Button><div className="demo-box"><strong>Tài khoản demo</strong><span>Learner: learner@lexigo.local / Demo123!</span><span>Admin: admin@lexigo.local / Admin123!</span></div></form></section></div>}
