import{createContext,useContext,useState,type ReactNode}from'react';const C=createContext({theme:'learner',setTheme:(value:string)=>{void value}});export function ThemeProvider({children}:{children:ReactNode}){const[theme,setTheme]=useState('learner');return <C.Provider value={{theme,setTheme}}><div data-theme={theme}>{children}</div></C.Provider>}export const useTheme=()=>useContext(C);
