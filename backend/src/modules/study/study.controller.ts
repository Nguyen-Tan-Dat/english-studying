import type{Request,Response}from'express';import{studyService as s}from'./study.service.js';export const create=(q:Request,r:Response)=>r.status(201).json(s.create(q.user!.id,q.body));export const get=(q:Request,r:Response)=>r.json(s.get(q.user!.id,q.params.sessionId));export const answer=(q:Request,r:Response)=>r.json(s.answer(q.user!.id,q.params.sessionId,q.body));export const complete=(q:Request,r:Response)=>r.json(s.complete(q.user!.id,q.params.sessionId));export const pronunciation=(q:Request,r:Response)=>r.status(202).json(s.pronounce(q.user!.id,String(q.body.concept_id)));export const attempt=(q:Request,r:Response)=>r.json(s.attempt(q.user!.id,q.params.attemptId));
