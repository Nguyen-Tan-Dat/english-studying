import{z}from'zod';const source=z.object({type:z.enum(['TOPIC_NODE','SAVED_QUERY','WRONG_ANSWERS']),id:z.string().uuid()});export const createSessionSchema=z.object({mode:z.enum(['FLASHCARD','PRONUNCIATION','VI_TO_EN','EN_TO_VI','LISTEN_EN_WRITE_EN','LISTEN_EN_WRITE_VI','MULTIPLE_CHOICE']),source,limit:z.number().int().min(1).max(100).default(20),shuffle:z.boolean().default(true)});export const answerSchema=z.object({item_id:z.string().uuid(),answer:z.record(z.unknown()),response_ms:z.number().int().min(0).optional()});
