import{z}from'zod';export const previewSchema=z.object({scope:z.enum(['TREE','BRANCH']),root_node_id:z.string().uuid().nullable().optional(),draft_revision:z.number().int().min(1).optional()});export const publishSchema=previewSchema.extend({draft_revision:z.number().int().min(1),display_name:z.string().max(140).nullable().optional(),description:z.string().max(500).nullable().optional(),audit_note:z.string().max(300).nullable().optional()});export const cloneSchema=z.object({display_name:z.string().max(120).nullable().optional(),selected_root_node_id:z.string().uuid().nullable().optional(),audit_note:z.string().max(300).nullable().optional()});
