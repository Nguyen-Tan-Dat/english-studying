import{store}from'../../infrastructure/store/memory.store.js';export class PublishingService{preview(u:string,t:string,b:any){return store.publicationPreview(u,t,b)}publish(u:string,t:string,b:any){return store.publish(u,t,b)}list(u:string,t:string,q:any){return store.listPublications(u,t,q)}unpublish(u:string,id:string){return store.unpublish(u,id)}library(q:any){return store.listPublicLibrary(q)}publicItem(id:string){return store.getPublicItem(id)}publicNodes(id:string,q:any){return store.listPublicNodes(id,q.parent_id??null,Number(q.limit??50),q.cursor)}clone(u:string,id:string,b:any){return store.clonePublic(u,id,b)}}export const publishingService=new PublishingService();
