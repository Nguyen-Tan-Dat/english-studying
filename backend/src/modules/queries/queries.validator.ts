import{z}from'zod';const alias=z.object({alias:z.string().regex(/^[A-Za-z_][A-Za-z0-9_]{0,19}$/),topic_node_id:z.string().uuid(),topic_display_name:z.string().nullable().optional()});export const parseSchema=z.object({expression:z.string().max(2000),aliases:z.array(alias).min(1).max(100),universe:z.enum(['ALIASES_UNION','ALL_ACCESSIBLE']).default('ALIASES_UNION'),implicit_and:z.boolean().default(false)});export const previewSchema=parseSchema.extend({page:z.number().int().min(1).default(1),page_size:z.number().int().min(1).max(100).default(20),sort:z.string().default('english_asc')});export const saveSchema=z.object({display_name:z.string().min(1).max(120),mode:z.enum(['DYNAMIC','SNAPSHOT']),visibility:z.enum(['PRIVATE','PUBLISHED']).default('PRIVATE'),expression:z.string().max(2000),aliases:z.array(alias).min(1).max(100),universe:z.enum(['ALIASES_UNION','ALL_ACCESSIBLE'])});
