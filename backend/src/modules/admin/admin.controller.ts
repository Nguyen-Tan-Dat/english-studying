import type{Request,Response}from'express';import{adminService as s}from'./admin.service.js';export const dashboard=(_q:Request,r:Response)=>r.json(s.dashboard());export const trees=(q:Request,r:Response)=>r.json(s.trees(q.user!.id,q.query));export const create=(q:Request,r:Response)=>r.status(201).json(s.create(q.user!.id,q.body));export const health=(_q:Request,r:Response)=>r.json(s.health());export const audit=(q:Request,r:Response)=>r.json(s.audit(q.query));
