import {store} from '../../infrastructure/store/memory.store.js'; export class VocabularyService{list(u:string,n:string,q:any){return store.listVocabularies(u,n,q)}create(u:string,n:string,b:any){return store.createVocabulary(u,n,b)}get(u:string,id:string){return store.getVocabulary(u,id)}version(id:string){return store.getVocabularyVersion(id)}update(u:string,id:string,b:any){return store.updateVocabulary(u,id,b)}delete(u:string,id:string){return store.deleteVocabulary(u,id)}search(u:string,q:any){return store.searchVocabulary(u,q)}} export const vocabularyService=new VocabularyService();
