import {z} from 'zod'; export const vocabularySchema=z.object({english:z.string().min(1).max(100),vietnamese:z.string().min(1).max(250),sense_key:z.string().max(100).optional(),pronunciation:z.string().max(120).nullable().optional(),part_of_speech:z.enum(['NOUN','VERB','ADJECTIVE','ADVERB','PHRASE','OTHER']).nullable().optional(),example:z.string().max(500).nullable().optional(),image_url:z.string().url().nullable().optional()});
