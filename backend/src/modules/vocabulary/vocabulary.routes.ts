import{Router}from'express';import{requireAuth}from'../../shared/middlewares/auth.guard.js';import{asyncHandler}from'../../shared/middlewares/async-handler.js';import{validate}from'../../shared/middlewares/validate.js';import{vocabularySchema}from'./vocabulary.validator.js';import * as c from'./vocabulary.controller.js';export const vocabularyRouter=Router();vocabularyRouter.get('/topic-nodes/:nodeId/vocabularies',requireAuth,asyncHandler(c.list));vocabularyRouter.post('/topic-nodes/:nodeId/vocabularies',requireAuth,validate({body:vocabularySchema}),asyncHandler(c.create));vocabularyRouter.get('/vocabularies/:vocabularyId',requireAuth,asyncHandler(c.get));vocabularyRouter.patch('/vocabularies/:vocabularyId',requireAuth,validate({body:vocabularySchema}),asyncHandler(c.update));vocabularyRouter.delete('/vocabularies/:vocabularyId',requireAuth,asyncHandler(c.remove));vocabularyRouter.get('/search/vocabularies',requireAuth,asyncHandler(c.search));
