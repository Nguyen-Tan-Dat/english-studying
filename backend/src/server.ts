import{app}from'./app.js';import{env}from'./config/env.js';import{logger}from'./config/logger.js';import{closeDatabase}from'./infrastructure/database/client.js';const server=app.listen(env.PORT,()=>logger.info({port:env.PORT,prefix:env.API_PREFIX},'LexiGo API started'));async function shutdown(signal:string){logger.info({signal},'Graceful shutdown');server.close(async()=>{await closeDatabase().catch(()=>undefined);process.exit(0)});setTimeout(()=>process.exit(1),10_000).unref()}process.on('SIGTERM',()=>void shutdown('SIGTERM'));process.on('SIGINT',()=>void shutdown('SIGINT'));
